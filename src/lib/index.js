// place files you want to import through the `$lib` alias in this folder.

/*
 * In using the $lib alias, here is how you'd use it.
 *
 * lib/index.js:
 * export const message = "Hello World";
 *
 * routes/+page.svelte:
 * <script>
 *   import { message } from '$lib';
 * </script>
 *
 * <p>{message}</p>
 */
